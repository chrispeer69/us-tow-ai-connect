@@ -29,7 +29,7 @@
   );
 
   const port = process.env.PORT || 3001;
-  await app.listen(port);
+  await app.listen(port, '0.0.0.0');
   Logger.log(`[Bootstrap] US Tow AI-Connect API listening on :${port}`);
 }
 
