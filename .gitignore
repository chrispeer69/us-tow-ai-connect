node_modules/
dist/
.next/
.env
.env.*
!.env.example
*.log
.DS_Store
coverage/
.turbo/
*.tsbuildinfo

# S29 — VAPID operator follow-up contains private keys, never commit
docs/sessions/S29_OPERATOR_TODO.md
# ---- Session 41: security sweep ----
# Diagnostic screenshots may contain portal/session data
docs/diagnostics/*.png
# Explicit (already covered by .env.* above; listed for clarity)
.env.local
# Scheduler lock — local runtime artifact, never commit
.claude/scheduled_tasks.lock
# gitleaks binary + raw report (report embeds matched strings)
scripts/security/bin/
docs/security/gitleaks-report.json
# Adapter discovery screenshots may contain real customer PII — keep them local.
